services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    user: "${UID:-1000}:${GID:-1000}"
    container_name: next-erp-app
    restart: unless-stopped
    volumes:
      - .:/var/www/html
    depends_on:
      db:
        condition: service_healthy
    networks:
      - next-erp

  web:
    image: nginx:alpine
    container_name: next-erp-web
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - next-erp

  db:
    image: postgres:17-alpine
    container_name: next-erp-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - next-erp-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - next-erp

  mailhog:
    image: mailhog/mailhog
    container_name: next-erp-mailhog
    restart: unless-stopped
    ports:
      - "8025:8025"
      - "1025:1025"
    networks:
      - next-erp

  adminer:
    image: adminer
    container_name: next-erp-adminer
    restart: unless-stopped
    environment:
      ADMINER_DEFAULT_SERVER: db
    ports:
      - "8080:8080"
    depends_on:
      - db
    networks:
      - next-erp

networks:
  next-erp:
    driver: bridge

volumes:
  next-erp-db:
