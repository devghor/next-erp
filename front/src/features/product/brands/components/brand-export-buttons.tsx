'use client';

import { useMutation } from '@tanstack/react-query';
import { toast } from 'sonner';
import { ExportButton } from '@/components/buttons/export-button';
import { exportBrandsPdf, exportBrandsExcel } from '../api/service';
import { useBrandFilters } from '../hooks/use-brand-filters';

function downloadBlob(blob: Blob, filename: string) {
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;
  link.download = filename;
  link.click();
  URL.revokeObjectURL(url);
}

export function BrandExportButtons() {
  const filters = useBrandFilters();

  const pdfMutation = useMutation({
    mutationFn: () => exportBrandsPdf(filters),
    onSuccess: (blob) => downloadBlob(blob, `brands-${Date.now()}.pdf`),
    onError: () => toast.error('Failed to export PDF')
  });

  const excelMutation = useMutation({
    mutationFn: () => exportBrandsExcel(filters),
    onSuccess: (blob) => downloadBlob(blob, `brands-${Date.now()}.xlsx`),
    onError: () => toast.error('Failed to export Excel')
  });

  const isPending = pdfMutation.isPending || excelMutation.isPending;

  return (
    <ExportButton
      onExportPdf={() => pdfMutation.mutate()}
      onExportExcel={() => excelMutation.mutate()}
      isPending={isPending}
    />
  );
}
